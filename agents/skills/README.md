# Skills (generated)

Skill directories here are **symlinks** created by `pnpm setup:agent-links` from [agent-kit](https://github.com/rcrdk/agent-kit) (`.agents/agent-kit/skills/`).

Edit bundled skills in agent-kit, not here. Run `pnpm setup:agents` after clone or submodule update.
