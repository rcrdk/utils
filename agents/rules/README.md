# Rules (generated)

Rule files here are **symlinks** created by `pnpm setup:agent-links` from the [agent-kit](https://github.com/rcrdk/agent-kit) submodule (`.agents/agent-kit`).

- `rules/shared/` — all projects
- `rules/utils/` — project overrides (this repo)

Edit rules in [agent-kit](https://github.com/rcrdk/agent-kit), not here. Run `pnpm setup:agents` after clone or submodule update.

See [agents/README.md](../README.md) for the rules index.
